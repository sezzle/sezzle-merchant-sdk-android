<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application>
        <!-- Redirect handler for the SYSTEM_BROWSER (Chrome Custom Tabs) checkout path.
             Catches the SDK's default `sezzle-sdk://checkout/` redirects and forwards them
             back to the launching activity via CheckoutHandler.handleCallbackUri. -->
        <activity
            android:name=".checkout.SezzleRedirectActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="sezzle-sdk"
                    android:host="checkout" />
            </intent-filter>
        </activity>

        <!-- WebView checkout activity — used when mode is WEB_VIEW -->
        <activity
            android:name=".checkout.SezzleCheckoutWebViewActivity"
            android:exported="false"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoTitleBar" />
    </application>

</manifest>
