<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required to query browser version for Auth Tab support check -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application>
        <!-- Fallback redirect handler for browsers that don't support Auth Tab (Chrome < 137).
             When Auth Tab IS supported, this activity is never triggered. -->
        <activity
            android:name=".checkout.SezzleRedirectActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="sezzle-sdk"
                    android:host="checkout" />
            </intent-filter>
        </activity>

        <!-- WebView checkout activity — used when mode is WEB_VIEW -->
        <activity
            android:name=".checkout.SezzleCheckoutWebViewActivity"
            android:exported="false"
            android:theme="@android:style/Theme.NoTitleBar" />
    </application>

</manifest>
