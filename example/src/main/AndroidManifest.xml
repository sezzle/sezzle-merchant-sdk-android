<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".ExampleApplication"
        android:allowBackup="false"
        android:label="Sezzle SDK Example"
        android:theme="@style/Theme.SezzleExample">
        <activity
            android:name=".ProductActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ResultActivity" />

        <!--
            Reference for merchants shipping their own callback scheme with
            system-browser mode (Chrome <137 / Custom Tabs fallback): the SDK's bundled
            manifest only covers `sezzle-sdk://checkout`, so any other scheme needs an
            entry like this in the merchant's own AndroidManifest.

            Registered but deliberately unused by the demos. `POST /v2/session` validates
            callback URLs against a server-side scheme allowlist and rejects anything
            outside it with a 400, and `sezzle-example` is not on that list — so the
            manifest entry alone is not sufficient. A merchant's scheme has to be
            allowlisted by Sezzle before it works end to end.
        -->
        <activity
            android:name="com.sezzle.sdk.checkout.SezzleRedirectActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="sezzle-example"
                    android:host="checkout" />
            </intent-filter>
        </activity>
    </application>

</manifest>
